<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Wiki</title>
    <link rel="stylesheet" href="/css/site.css">
</head>
<header>
   {{#if error}}
  <div class="error">{{error}}</div>
  {{/if}}
  {{#if message}}
  <div class="success">{{message}}</div>
  {{/if}}
</header>
<body>
    <div>
        <aside>
            <div class="center">
                <img class="logo" src="/img/logo.png" alt="logo">
            </div>

            <nav>
                <a href="/">Main Page</a>
                <a href="/articles/getLatestArticle">Latest Article</a>
                <a href="/articles/allArticles">All Articles</a>
                <form action="/search" method="POST">
                    <input type="text" name="searchedTitle">
                    <input type="submit" value="Search">
                </form>
                <a href="/articles/addArticle">Create New Article</a>
            </nav>
        </aside>
        <div id="container">
            <header>
              {{#if currentUser}}
               <span>Welcome, {{currentUser.username}}!</span>
                 <form id="logout-form" action="/users/logout" method="POST"></form>
                <a href="javascript:document.getElementById('logout-form').submit()">Logout</a>
              {{else}}
                <a href="/users/login">Login</a>
                <a href="/users/register">Register</a>
              {{/if}} 
            </header>
            <main>
              <h1>Welcome to SoftUni Wiki!</h1>
                 {{#if globalError}}
                <h3>{{globalError}}</h3>
                {{/if}}
                {{{body}}}
            </main>
            <footer>
                SoftUni Wiki &copy; 2018
            </footer>
        </div>
    </div>
</body>

</html>
